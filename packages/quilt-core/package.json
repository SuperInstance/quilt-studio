{
  "name": "@quilt-studio/core",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "L1 kernel contract + adapters for quilt-studio. Reference JS kernel and real quilt-vm-wasm WASM adapter both pass tests/contract-suite.mjs (28 tests each).",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build:kernel": "bash scripts/build-kernel.sh"
  },
  "license": "EPL-2.0"
}
