{
  "name": "@quilt-studio/view",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RTS-style live view of the quilt cell graph: kernel subscription → render model → frame diffs → canvas. The engine the skeleton promised.",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "license": "EPL-2.0",
  "peerDependencies": {
    "@quilt-studio/core": "*"
  }
}
